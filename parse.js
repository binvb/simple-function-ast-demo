function getNode(tokens) {
    let current = 0

    while(current < tokens.length) {
        if(tokens[current] === 'function') {

        }
    }
}
export function parse(tokens) {
    let _length = tokens.length
    
    while() {

    }
}